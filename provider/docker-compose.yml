version: '3'
services:
  provider:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    container_name: msc-thug-provider
    ports:
      - "8888:8000"
    volumes:
      - .:/usr/src/appProvider